"""TraceLink — link engineering findings to the code they name.

The package keeps the three operations as separate modules because they are
separately useful: an index can be built without a vault, and a vault can be
split without ever linking it.
"""

__version__ = "0.5.1"
